
export interface Partner {
  name: string;
  logo?: string;
}

export interface GalleryItem {
  id: string;
  url: string;
  caption: string;
}

export interface TrainingDetail {
  label: string;
  value: string;
  icon: string;
}
